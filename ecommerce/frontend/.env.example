# Optional: full API base including /api (only for split deploy: frontend on CDN, API elsewhere)
# Example: VITE_API_URL=https://your-api.onrender.com/api
# Leave unset when the API is on the same origin (single server serves /api + static app).
